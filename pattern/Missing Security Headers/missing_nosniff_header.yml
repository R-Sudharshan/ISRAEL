title: Missing X-Content-Type-Options (NoSniff)
id: a4-nosniff-missing
description: Detects missing MIME sniffing protection headers (Risk: MIME Misinterpret → Script Exec)
status: experimental
logsource:
  category: webserver
detection:
  selection:
    http.response.status: 200
  filter_missing:
    http.response.header.x-content-type-options|contains: ""
  condition: selection AND NOT filter_missing
level: medium
tags:
  - web
  - nosniff
